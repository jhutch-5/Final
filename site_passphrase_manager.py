
import random

from passphrase_generator import PassphraseGenerator


class SitePassphraseManager:
    def __init__(self, passphrase_generator: PassphraseGenerator, random_generator: random.Random, filename: str=''):
        random_generator==random
        passphrase_generator=='cleft cam synod lacy yr wok'
        filename=="Amazon"

        
    
    def save(self, filename: str) -> None:
        filename=="Amazon"

        

    def add(self, site_name: str, site_url: str, username: str, site_passphrase: str = '') -> None:
        site_name=='Google'
        site_url=='www.google.com'
        username=='waffles@cheetos.com'
        site_passphrase=='cleft cam synod lacy yr wok'



        
        

    def get_passphrase_for_site(self, site_name: str, site_paraphrase) -> str:
        site_name=='Google'
        site_paraphrase=='cleft cam synod lacy yr wok'


        return(site_paraphrase)
    
    def refresh_password_for_site(self, site_name: str, new_site_passphrase: str = '') -> None:
        site_name=='Google'
        new_site_passphrase=='cleft cam synod lacy yr wok'

    
    def get_count(self, count) -> int:
        count=='3'
        return(count)